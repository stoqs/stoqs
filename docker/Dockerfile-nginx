FROM docker.io/nginx:1.13

ARG NGINX_TMPL

# Add start script
ADD nginx-start.sh /
RUN chmod +x nginx-start.sh

# Add nginx config file
ADD ${NGINX_TMPL} /

# SSL cert files now mounted from the host to locations specified in nginx.conf

CMD ["./nginx-start.sh"]
