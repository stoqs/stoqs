services:
  docs:
    image: stoqs_local_docs
    container_name: stoqs_local_docs
    build:
      context: .
      dockerfile: ./compose/local/docs/Dockerfile
    env_file:
      - ./.envs/.local/.django
    volumes:
      - ./docs:/docs:z
      - ./config:/app/config:z
      - ./stoqs:/app/stoqs:z
    ports:
      - '9000:9000'
    command: /start-docs
